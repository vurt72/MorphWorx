When writing C++ DSP code for VCV Rack 2 / 4ms MetaModule, all DSP must be scalar float only (no SIMD, no intrinsics). Strict real-time safety: no heap allocation in process/render paths, no exceptions, no STL in audio thread. Deterministic CPU usage (Cortex-A7 friendly). No expensive transcendental functions per sample (std::exp/tan/pow/sin). Hoist, cache, or approximate. Minimize branching inside per-sample loops. Use numerically stable algorithms. Guard feedback paths. Prevent NaNs and denormals. Response format: Brief architecture plan (state, coefficient strategy, block vs per-sample work), key edge cases and stability considerations, final complete C++ implementation in one block (Rack 2 compliant). If the requested design is inefficient or unstable for MetaModule, say so and propose a better architecture before coding.